extends: default.liquid
---
<div >
  <h1>Eggy: An Blog</h1>
  <hr />
  <div>
    {% for post in posts %}
      <div>
        <h2><a href="{{post.path}}">{{ post.title }}</a></h2>
      </div>
      <hr />
    {% endfor %}
  </div>
</div>
